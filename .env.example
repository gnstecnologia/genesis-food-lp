# GoHighLevel Private Integration
GHL_API_TOKEN=pit-xxxxxxxx
GHL_LOCATION_ID=3PX64HuGchlu0IkZet5Z

# Tag aplicada no contato quando agenda no Cal.com
GHL_BOOKING_TAG=agendado

# Opcional: webhook inbound do GHL para disparar workflow
GHL_WEBHOOK_URL=https://services.leadconnectorhq.com/hooks/3PX64HuGchlu0IkZet5Z/webhook-trigger/YXnc32VOmV3WvZmpuQfs

# Tag MQL (formulário) — a partir de "50 mil até 80 mil" (não MQL: Até 30 mil / 30–50 mil)
# Após o webhook, a API espera ~3 min, busca o lead e aplica a tag
GHL_MQL_TAG=MQL

# URL pública do endpoint (Vercel)
# Cal.com → Settings → Developer → Webhooks → BOOKING_CREATED
# https://genesis-food-lp.vercel.app/api/cal-webhook
# Form → tag MQL: https://genesis-food-lp.vercel.app/api/tag-mql
